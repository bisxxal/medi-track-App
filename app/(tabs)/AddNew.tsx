import React from 'react'
import { Image, ScrollView, Text, View } from 'react-native'
import AddMedicine from '../add-medicine' 
const Addnewr = () => {
  return (
    <ScrollView className='p-2 relative'> 
     <AddMedicine/>  
    </ScrollView>
  )
}

export default Addnewr